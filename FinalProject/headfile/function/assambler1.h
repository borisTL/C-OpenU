/*
=======================================================================================================================================================================							
								    Assambrel.h
=======================================================================================================================================================================

 	|In this file, the assembler function is declared, which receives as operands the number
 	|of files on the command line and their names, inside the function, the file processing
 	|function will be called to display files .ob .ext .ent.
 	|Implementation is in assambler.c
*/

/*	|Assembler function gets number of files and names,using this function we take the names and send them for verification.
*/	 void assambler(int argc, char* argv[]);



